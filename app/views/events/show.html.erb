<%= render 'layouts/navbar' %>

  <div class="imagestockpile">
      <div id="slider" class="carousel carousel-slide" data-transition="slide">
    <% @photo_detail.each do |photo| %>
        <div class="carousel-item"><img src="<%= photo["url"] %>"/></div>
      <%= photo["title"] %>
      <!-- <%= photo["tags"] %> -->
    <% end %>
      </div>
  </div>
</div>
